import { createMessages } from '../core';

export const versionWarningMessages = createMessages({
  showVersionChangelog: {
    defaultMessage: 'Show version changelog',
  },
  versionChangelog: {
    defaultMessage: 'Version Changelog',
  },
});
